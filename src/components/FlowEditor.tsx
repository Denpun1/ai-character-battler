
"use client";

import React from 'react';

export function FlowEditor() {
  return (
    <div style={{ height: '600px', background: 'rgba(0,0,0,0.2)', borderRadius: '12px', display: 'flex', alignItems: 'center', justifyItems: 'center', border: '1px dashed #444' }}>
      <div style={{ margin: 'auto', textAlign: 'center', opacity: 0.5 }}>
        <div style={{ fontSize: '3rem', marginBottom: '1rem' }}>🔗</div>
        <h3>Logic Flow Editor</h3>
        <p>Flow-based programming (React Flow) implementation in progress...</p>
      </div>
    </div>
  );
}
